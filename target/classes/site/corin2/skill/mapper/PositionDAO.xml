<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.corin2.skill.dao.PositionDAO">
<!-- 	<insert id="positionInsert" parameterType="site.corin2.skill.dto.positionDTO">
		insert into position(projectnum,userid,skillnum,x,y,width,height)
		values(#{projectNum},#{userId},#{skillNum},#{x},#{y},#{width},#{height})
	</insert>
	
	<update id="userUpdate">
		update position
		set username=#{userName},password = #{password}, userprofile=#{userProfile}
		where userid = #{userId}
	</update>
	
	<dedate id="positionDelete">
		dedate position
		set 
		where userid = #{userId}
	</dedate>
	
	<select id="positionAllSelect" resultType="site.corin2.skill.dto.positionDTO">
		select * from position where =#{userId}
	</select>
	
	<select id="positionSelect" resultType="site.corin2.skill.dto.positionDTO">
		select * from position where =#{userId}
	</select>
 -->
</mapper>